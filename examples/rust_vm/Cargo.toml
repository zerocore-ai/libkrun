[package]
name = "rust_vm"
version = "0.1.0"
edition = "2021"
publish = false

[features]
tee = ["msb_krun/tee"]
amd-sev = ["tee", "msb_krun/amd-sev"]
tdx = ["tee", "msb_krun/tdx"]

[dependencies]
env_logger = "0.11"
msb_krun = { path = "../../src/krun" }
